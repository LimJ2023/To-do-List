동적 페이지 설정

<a href="/read/1"> <--이런 경로들이 많이 있을 때..

app/경로명/[파라미터이름]/page.jsx로 만들고 안에

export defaul function 이름(props) <- 매개변수 설정해주고

{props.params.id}로 값을 불러올 수 있다.

주소는 이렇게 됨
localhost:3000/read/1 , 2, 3 ... 


서버 사이드 렌더링 ?
    의 단점을 싱글페이지 어플리케이션으로 극복

    Link를 사용.

이미지 같은 파일은 어떻ㄱ ㅔ사용할까?

    최상위 폴더에 public 폴더 생성 그 안에 <Image/> 컴포넌트를 사용.
    width와 height값이 필요함.